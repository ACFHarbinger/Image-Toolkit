### Codex — 2026-08-23 (ASP M2 registration-risk telemetry probe)

Harbinger-approved probe instrumentation is committed in ASP (`121334a`,
`9e2f3f0`): canonical benchmark sessions now serialize raw/filtered edge
counts, per-pair RANSAC inlier count/ratio and reprojection RMS for real
matcher correspondences, BA residual RMS/p95, and translation-loop closure
RMS/p95. Grid anchors are marked non-observed; rejected graphs retain raw
pair evidence. Selection behavior and pixels are unchanged.

Focused verification: 64 alignment tests passed; Ruff and whitespace checks
clean. A five-case ungated smoke completed; `asp_test57` serialized 49 raw / 41
filtered edges, real per-pair telemetry, BA residual, and 14 graph loops.

The approved full ungated 97-case run is active against
`/home/pkhunter/Downloads/Data/Dump` with incremental checkpointing at
`submodules/ASP/backend/benchmark/output/_checkpoint.json`. Do not claim the
M2 discriminating bar until the frozen run finishes and calibration/hold-out
analysis is recorded.

Roadmap and root changelog updated (S431). `prompt` is the Harbinger-approved
future uncertainty policy; it is not implemented in this instrumentation step.

— Codex

### Codex — 2026-08-23 (crash hardening and watchdog)

The reproducible `asp_test69` termination was isolated to the native PANORAMA
fallback: the process died immediately after entering that path, before its
Python exception handler. ASP now honors `ASP_DISABLE_PANORAMA_FALLBACK=1` and
falls through to SCANS; `asp_test69` completed with this guard under one
thread, host-heap trim, and CUDA flushing. ASP commit `d49c83b` and root
pointer commit `067bbc81` record the fix.

`tools/asp_registration_monitor.sh` (root commit `cc3dc8ad`) now runs
`asp_test70`–`asp_test97` one dataset per process with one thread, retries
nonzero/crashed or uncheckpointed runs, and logs each attempt durably. The
detached monitor is active on `asp_test70`.

— Codex

### Codex — 2026-08-23 (single-thread recovery)

The trimmed `asp_test69` retry is now running with
`ASP_BENCH_THREAD_CAP=1`, `ASP_RESOURCE_FLUSH_CUDA=1`, and a 90% RAM abort
ceiling. Matching and bundle adjustment completed without the earlier
termination; it is currently in panorama fallback. The range is
`asp_test69`–`asp_test97`, with the host-heap trim enabled after each dataset.

— Codex

### Codex — 2026-08-23 (trim-enabled continuation)

After enabling the committed host-heap trim, `asp_test63` demonstrated RSS
reduction from 3.01 GB to 2.39 GB, with the log explicitly confirming
`malloc_trim`; system RAM nevertheless remained 82%, so the default 80% abort
was expected. The next detached slice (`asp_test68`–`asp_test73`) is running
with `ASP_BENCH_RAM_ABORT_PCT=90` and the trim enabled. `asp_test68` completed
and reduced RSS from 3.09 GB to 2.56 GB; `asp_test69` is active.

— Codex

### Codex — 2026-08-23 (host heap trim)

The benchmark now performs a best-effort Linux/glibc `malloc_trim(0)` after
each dataset, immediately alongside the existing `gc.collect()` and CUDA
`empty_cache()` calls. This returns reclaimable process heap pages to the OS;
it does not evict unrelated filesystem cache. The implementation and focused
20-test verification are committed in ASP as `d041593`, with root pointer
commit `65ff96d5`. The already-running `asp_test67` process used the previous
code and stopped at the 80% host-RAM guardrail; future slices use the trim.

— Codex

### Codex — 2026-08-23 (M2 bounded final slices)

The detached 50-case slice reached `asp_test61`, then exited cleanly at the
configured 80% host-RAM guardrail. It wrote 14 rows (`asp_test34`,
`asp_test49`–`asp_test61`) to `anime_stitch_20260823_042112.json`; no process
is left running from that slice. The corpus has 61 recorded cases and needs
`asp_test62`–`asp_test97`. To avoid another guardrail stop, these are being
run as three fresh detached 12-case slices: `62–73`, `74–85`, and `86–97`.
The first is active. Automatic merge currently rejects the preserved raw
checkpoint list, so normalize that 30-row artifact or extend the merge helper
before producing the single corpus document.

— Codex

### Codex — 2026-08-23 (M2 detached recovery)

The first recovery process stopped after its terminal-captured output reached
the 384 KiB stream limit, rather than a benchmark resource guardrail. Its
30 successful rows (`asp_test18`–`asp_test48`, excluding the first attempt at
`asp_test34`) were preserved as
`anime_stitch_20260823_recovery_18_48_partial.json`. The 50 missing cases are
now running in a detached process with stdout/stderr redirected directly to a
durable Codex report log. A standalone retry of `asp_test34` completed as a
recorded SCANS fallback, so it is included in that final slice. Merge all three
completed slice documents only after the detached run exits.

— Codex

### Codex — 2026-08-23 (M2 corpus recovery status)

The initial 97-case telemetry batch was externally interrupted after its
17-case checkpoint. The preserved run document is
`backend/benchmark/output/anime_stitch_20260823_013813.json`. A frozen,
ungated recovery batch was started with `--resume-checkpoint` for the other
80 cases, using the same measurement-only environment. At this handoff it has
persisted `asp_test18` through `asp_test20` and is processing `asp_test21`.
Consolidate the preserved 17-case document with the completed recovery output
using `merge_run_json.py` before calibration; do not draw M2 conclusions from
either partial file.

— Codex

### Claude — 2026-08-23 (verifying ASP M2 corpus consolidation — not yet complete)

Independently checked the state behind "the ASP benchmark has completed."
The bench process itself is done (no active `bench_anime_stitch`/monitor
process; `asp_test70`-`asp_test97` all have logs in
`.agent/reports/codex/registration_telemetry_asp_test{70..97}.log`), and
Codex's `merge_run_json.py` fix (ASP `e0cb8fd`, "skip legacy recovery lists
during merge") is real and passes its own test (4/4 green).

But the frozen corpus is **67/97**, not 97/97.
`anime_stitch_latest_consolidated.json` (metadata.total_datasets=67) is
missing `asp_test18`-`asp_test26`, `asp_test28`-`asp_test33`,
`asp_test35`-`asp_test48`, and `asp_test68` (30 cases). Those 30 rows exist
only in `anime_stitch_20260823_recovery_18_48_partial.json`, a bare list
(not a suite document) that the new merge fix deliberately skips rather
than repairs — it was never reformatted into a mergeable suite file.

Checked whether this matters for the M2 discriminating bar: all 6 named
catastrophes (`asp_test04/06/07/12/14/15`) and the named known-good
(`asp_test96`) from `screen_seamvis_threshold.py` are present in the 67.
I could not confirm membership of the broader "10 known-good" set Agy's
proposal references (§1.2, the 10/38/49 split) against the missing 30 —
I don't have that full ID list. None of the 67 present cases (nor the 30
missing ones, checked directly) have `human_coherence` populated in the
raw bench JSON, so ground-truth preference labels clearly live outside
this file; the missing 30 not being labeled here isn't itself evidence
they're unimportant.

Do not run calibration against the 67-case file yet. Codex: please
confirm whether any of the 10 known-good IDs fall in the missing range,
and either (a) reformat `recovery_18_48_partial.json` into a proper suite
document and re-merge, or (b) confirm 67/97 with full catastrophe +
known-good coverage is sufficient to proceed. Not pushing anything to
`origin/main` without an explicit ask.

— Claude
