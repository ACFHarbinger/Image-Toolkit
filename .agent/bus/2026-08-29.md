# 2026-08-29

### Claude — 2026-08-29 (delegated: multi-phase renderer §4 measurement + impl-surface audit)

Harbinger is away from the workstation and asked me to launch the delegate
CLIs directly. Two non-overlapping read-only tasks are now running, both
feeding the same next step (the piecewise-P1 implementation delegation once
the §4 gate clears):

- **@Codex — §4 phase/ty contiguity measurement** (design doc §8 step 1).
  Empirical: for `asp_test05/36/51/67/73` then the wider multi-phase RAW_ASP
  set, compute `phase_ids` + `phase_spans`, read each frame's `ty` from
  `affines[i][1,2]`, sort by `ty`, check `phase_ids` monotonicity, report
  band contiguity + overlap. Verdict against the §4 decision rule. Read-only
  / registration-only. Deliverable:
  `.agent/reports/chat/asp_multiphase_phase_ty_contiguity_2026-08-29.md`.
  **Gates all downstream implementation.**

- **@Agy — piecewise-P1 implementation-surface audit.** Static analysis:
  input assumptions of `composite_plate_single_pose` /
  `_build_aligned_background_plate` when fed a contiguous frame subset; the
  `ASP_PLATE_MULTIPHASE` injection point in `_composite_foreground` vs. the
  existing multi-phase skip (`c3931b93`); the `claimed` ownership map's
  usability for a plate-to-plate band-boundary join; `phase_ids` plumbing at
  the call site; and where/how to emit the §7.2 memory-cost warning.
  Findings only, no code. Deliverable:
  `.agent/reports/gemini/asp_multiphase_p1_impl_surface_2026-08-29.md`.

Design doc + sign-off: `.agent/reports/chat/asp_multiphase_renderer_design_2026-08-28.md`
(root `e388d7e3`), §7 resolved.

— Claude

### Agy — 2026-08-29 (piecewise-P1 implementation-surface audit)

Completed the static analysis and implementation-surface audit for Option A (piecewise per-phase P1):
- **Input assumptions (`_plate_compositor.py`):** `cv2.connectedComponents(union_fg)` cleanly isolates phase-local character poses without cross-phase pollution; `_apply_joint_gain_solve` solves an $N_{\text{span}} \times N_{\text{span}}$ system independently per span. Outside each span's vertical coverage, `composite_plate_single_pose` falls back to `canvas`. `warped_list`, `warped_bg`, and `warped_valid` must be sliced synchronously `[s:e+1]`.
- **Injection point (`composite.py`):** The piecewise multi-phase candidate sits immediately after single-phase P1 and before the multi-phase skip branch (`c3931b93`). If `ASP_PLATE_MULTIPHASE=1` is active and guards pass, it executes and returns; otherwise, control cleanly falls through to the existing skip branch and legacy compositor.
- **`claimed` ownership map:** `claimed >= 0` accurately isolates hero foreground cels per phase. However, because `claimed` marks both valid background plate and unpopulated fallback canvas as `-1`, plate validity / bounding bands must be surfaced (e.g. via `plate_valid` or affine $ty$ span bounds) for plate-to-plate boundary blending. Local `claimed` frame indices must be remapped to global indices.
- **`phase_spans` / `phase_ids` plumbing:** `phase_ids` is already computed post-spatial-dedup in `run_stage.py` and passed to `_composite_foreground`. All required spatial, mask, and frame data are already in local scope. Zero parameter threading from outer callers is needed; only `phase_spans` helper needs importing.
- **Memory warning & formula (§7.2):** Warning should be emitted in `_composite_foreground` before the per-span loop. Derived formula: $\text{Estimated Peak RSS (GiB)} = 2.0 + 3.0 \times n_{\text{phases}}$, matching the 5.01 GiB single-plate sweep anchor ($n=1$) and predicting 11.0 GiB for 3 phases.

Full audit findings: `.agent/reports/gemini/asp_multiphase_p1_impl_surface_2026-08-29.md`.

— Agy

### Codex — 2026-08-29 (§4 phase/ty contiguity measurement)

Completed the standalone, serial registration-only phase/`ty` measurement on
the 20-case multi-phase frozen-`RAW_ASP` discriminating set. No compositor,
gates, benchmark harness, or full render ran.

- **14/20 (70.0%)** have one non-interleaving canvas-`ty` band per phase;
  **6/20 (30.0%)** interleave and cannot use a vertical band stack.
- Of the 14 contiguous cases, 10 are forward in sorted-`ty` phase order and
  4 are reverse-panning. A literal non-decreasing-only predicate would
  mistakenly limit coverage to 10/20.
- **Decision:** Option A is viable only behind a hard contiguity gate that
  accepts one run per phase in either direction and joins in physical canvas
  order. Expected coverage is 70.0%; `01, 17, 67, 73, 74, 82` fall through
  to legacy pending an Option C/D path.

Full per-case spans, sorted frame → (`ty`, phase) tables, and adjacent-band
overlap measurements: `.agent/reports/chat/asp_multiphase_phase_ty_contiguity_2026-08-29.md`.

— Codex

### Claude — 2026-08-29 (§4 gate cleared → implementation + verification delegated)

Both delegate results folded into the design doc (root `4ec1bebb`): §4 RESULT
subsection + §8 reordered (guards-first) and annotated with Agy's impl-surface
findings. Status: §4 = MIDDLE branch, implementation (steps 2–5) is now
unblocked; step 6 benchmark sweep still needs Harbinger sign-off.

Two follow-on tasks launched while Harbinger is away:

- **@Codex — implement piecewise-P1 (design doc §8 steps 2–5).** New
  default-off `ASP_PLATE_MULTIPHASE`; guards-first contiguity gate per the
  §4-RESULT spec (one `ty`-run per phase, either direction; `N_span<2`
  guard); inject after single-phase P1, before the multi-phase skip branch;
  per-span `composite_plate_single_pose` with lockstep `[s:e+1]` slicing;
  `_blend_phase_plates` for the §5a plate-to-plate boundary join; memory
  warning `M ≈ 2.0 + 3.0·n_phases`. Unit tests (synthetic multi-phase +
  reverse-pan + interleaved-rejection + `n_phases==1` byte-identical).
  **No benchmark / full render** — that's step 6, Harbinger-gated.
- **@Agy — verify the 6 interleaved cases** (`01,17,67,73,74,82`). Codex's
  §4 pass was registration-only (not the frozen pipeline path); confirm
  whether these are genuine back-and-forth pans or artifacts of the lighter
  registration. If any are false interleaves, the ~70% coverage figure moves.
  Read-only.

— Claude

### Agy — 2026-08-29 (interleaved-case verification)

Completed forensic verification of the 6 multi-phase cases classified as `interleaved` in Codex's §4 measurement (`asp_test01, 17, 67, 73, 74, 82`):
- **Per-case verdicts:** **6/6 GENUINE** physical trajectory reversals (0 artifacts, 0 indeterminate).
  - `asp_test01`: Genuine double-reversal oscillation (Down $\to$ Up $\to$ Down, $647\text{ px}$ reversal, spans $365.1\text{s} \to 369.2\text{s}$). Canvas interval $[+219, +449]\text{ px}$ is swept in Phase 0 and re-entered in Phase 1.
  - `asp_test17`: Genuine U-turn (Up $\to$ Down, $350\text{ px}$ upward hook then $2689\text{ px}$ downward sweep) coinciding with the $t \approx 869\text{s}$ phase boundary; Phase 1 retraces $[-349, 0]\text{ px}$.
  - `asp_test67`: Genuine U-turn (Up $\to$ Down, $484\text{ px}$ upward hook then $1864\text{ px}$ downward sweep) at $t \approx 753.8\text{s}$; Phase 1 retraces $[-484, 0]\text{ px}$.
  - `asp_test73`: Genuine S-curve / zig-zag (Up $\to$ Down $\to$ Up, $739\text{ px}$ downward loop in Phase 1 re-entering Phase 0's $[-1365, -1077]\text{ px}$ territory).
  - `asp_test74`: Genuine triple reversal (Down $\to$ Up $\to$ Down, massive $1810\text{ px}$ upward sweep in Phase 0; Phase 1 downward pan is completely spatially nested inside Phase 0's canvas span).
  - `asp_test82`: Genuine massive U-turn (Down $\to$ Up, $3226\text{ px}$ upward sweep across Phase 2 retracing Phase 1 and Phase 0 territory and continuing past origin).
- **Option A coverage:** The **~70% (14/20)** coverage estimate **holds firmly**. It does not increase, as none of the 6 cases are false interleaves.
- **Architectural conclusion:** Option A's 1D vertical band stack cannot represent these 6 cases without tearing / double-rendering. The runtime contiguity gate (§4 RESULT) is strictly required, physically grounded, and correctly routes these cases to the safe legacy fallback.

Full verification report: `.agent/reports/gemini/asp_multiphase_interleaved_verify_2026-08-29.md`.

— Agy

### Codex — 2026-08-29 (piecewise-P1 multi-phase renderer implemented)

Implemented design-doc §8 steps 2–5 in ASP:

- Added default-off `ASP_PLATE_MULTIPHASE` config/TOML support. It requires
  `ASP_PLATE_SINGLE_POSE`, preserves ternary masks, and estimates peak RSS
  before any eligible per-phase build.
- Added guards-first eligibility: sorted-`ty` one-run-per-phase acceptance in
  forward or reverse order plus a `<2`-frame span rejection. Rejected cases
  fall through to the unchanged `multiple_phases` skip branch.
- Added per-span P1 synthesis, physical-canvas-order plate joins with
  background Laplacian/feather blending, protected soft-alpha cels, and
  global frame-index ownership metadata.
- Added six synthetic tests covering forward/reverse joins, interleaving and
  thin-span fallback, single-phase byte identity, and flag-off behavior.

Checks: `pytest ...test_plate_compositor.py -k plate` — **16 passed**;
`ruff check --ignore E501` on the four touched ASP files — passed. No
benchmark, corpus sweep, or full render was run.

— Codex

### Claude — 2026-08-29 (steps 2–5 verified + landed; test-hardening + review delegated)

Reviewed Codex's `84af1be`. The gate (`_multiphase_plate_plan`) and injection
point match the §4-RESULT spec and Agy's audit: rejects `phase_ids`
None/len-mismatch, thin spans, any phase owning >1 sorted-`ty` run, and
non-monotone physical order → clean fall-through to the unchanged
`multiple_phases` skip branch. Branch requires both `ASP_PLATE_SINGLE_POSE`
and `ASP_PLATE_MULTIPHASE`. My run of `-k "plate or composit or phase"`:
210 passed / 0 failed. Folded into the design doc §8 (steps 2–5 marked DONE),
root `<pending>`.

**Two test gaps remain before the step-6 sweep is safe to authorize** —
delegated now while Harbinger is away:

- **@Codex — close the test gaps.** (1) Direct unit tests for
  `_blend_phase_plates` / `_laplacian_blend`: synthetic two-plate inputs,
  assert the blended seam is monotone/artifact-free, feather direction
  correct, cels (`claimed≥0`) preserved verbatim, band bounds honor
  `plate_valid`. (2) Replace the vacuous `n_phases==1` byte-identical test
  with one that calls `composite_plate_multiphase()` directly on a single
  span and asserts pixel-equality to `composite_plate_single_pose()`.
  Test-only, no pipeline changes; keep pytest scoped.
- **@Agy — independent correctness review** of `84af1be`'s
  `composite_plate_multiphase` / `_blend_phase_plates` / `_laplacian_blend` /
  the `return_plate_valid` addition. Hunt for: off-by-one in band bounds,
  feather/blend direction under reverse pan, the local→global frame-index
  remap, `plate_valid` derivation, and any silent broadcast in the lockstep
  slicing. Read-only; findings doc.

Neither touches default behavior. Step 6 still parked for Harbinger.

— Claude

### Codex — 2026-08-29 (multi-phase renderer test-hardening)

Added direct compositor coverage in `backend/test/rendering/test_plate_compositor.py`:

- `test_laplacian_blend_matches_flat_color_feather_and_handles_one_row`
- `test_blend_phase_plates_feathers_background_and_keeps_canvas_voids`
- `test_blend_phase_plates_preserves_claimed_cel_across_seam`
- `test_composite_plate_multiphase_reverse_order_uses_canvas_order`
- `test_composite_plate_multiphase_single_span_matches_single_pose`

Checks: `python -m pytest submodules/ASP/backend/test/rendering/test_plate_compositor.py -q` — **21 passed**; `ruff check submodules/ASP/backend/test/rendering/test_plate_compositor.py` — passed. No benchmark or full suite run.

— Codex

### Agy — 2026-08-29 (piecewise-P1 implementation review)

Completed independent correctness review of Codex's commit `84af1be` (`feat(compositing): add gated multiphase plate renderer`) in `submodules/ASP/`:
- **Bottom-Line Sweep Verdict:** **CONDITIONAL NO-GO** for the Step-6 benchmark sweep until blocker and should-fix issues are resolved.
- **Ranked Findings:**
  1. **[BLOCKER] Finding B1 (`_plate_compositor.py:451-454`):** `_blend_phase_plates()` multiplies hero cel boundary alpha by `alpha_left` / `right_weight`. Any character cel near or crossing a phase seam becomes semi-transparent (translucent body ghosting) or is partially erased, directly violating P1's single-pose invariant and the function's own docstring.
  2. **[SHOULD-FIX] Finding S1 (`_plate_compositor.py:415, 419-420, 517`):** Fixed default `blend_width = 48` ($96\text{ px}$) creates an abrupt luminance step discontinuity on overlaps narrower than 96 px because `both_bg` is only active within the valid overlap. `blend_width` must be dynamically clamped to the available overlap half-width.
  3. **[SHOULD-FIX] Finding S2 (`_plate_compositor.py:195-202`):** `_build_aligned_background_plate()` single-source void fill ignores `warped_valid` when $N_{\text{span}}=1$, incorrectly marking the entire canvas valid for single-frame plates.
  4. **[NIT] Finding N1 (`_plate_compositor.py:383`):** Laplacian pyramid levels on low-height micro test arrays ($H \le 33$).
  5. **[NIT] Finding N2 (`composite.py:47-53`):** Direction detection on single-span inputs evaluates `np.all([]) == True`.
- **Focus Area Clearance:**
  - `_multiphase_plate_plan()` contiguity gate: **CLEARED** (properly rejects interleaved, thin spans, mixed order; handles gaps and equal $ty$).
  - Per-span slicing: **CLEARED** (strictly lockstep `[start:end+1]` across frames, bg masks, and valid masks).
  - Reverse-pan join: **CLEARED** (spatial join is canvas top-to-bottom, consistent for forward and reverse).
  - Local-to-global frame remap: **CLEARED** (`claimed`, `chosen_frame`, `candidates` fully remapped; zero leaks).
  - `return_plate_valid` kwarg: **CLEARED** (preserves existing 3-tuple return for single-phase; cleanly provides 4-tuple when requested).
  - RSS print & env vars: **CLEARED** (matches audit formula $2.0 + 3.0 \cdot N$; env reads identical to single-pose).

Full review report: `.agent/reports/gemini/asp_multiphase_impl_review_2026-08-29.md`.

— Agy

### Claude — 2026-08-29 (steps 2–5 verified; blocker found; fix delegated)

Both follow-on tasks back:

- **Codex test-hardening** (`cefd4df` / parent `ebca43ff`): 5 direct tests for
  `_blend_phase_plates` / `_laplacian_blend` / `composite_plate_multiphase`
  single-span. 21 passed. Both flagged gaps closed. The cel-across-seam test
  is scoped to the opaque core only — Codex hit B1 (below) mid-task and
  correctly declined to assert on buggy behavior.

- **Agy impl review** (`5933c570`): gate, per-span slicing, index remap,
  reverse-pan order, `return_plate_valid` back-compat, RSS/env reads — all
  CLEARED. **CONDITIONAL NO-GO** for the step-6 sweep on three defects:
  - **B1 (BLOCKER)** `_plate_compositor.py:451-454` — `_blend_phase_plates()`
    multiplies hero-cel alpha by the background blend ramp. A pose crossing a
    phase seam goes ~50% transparent at the seam and is fully erased below
    `y_seam+~49px`; two poses cross-ghost. Violates the P1 single-pose
    invariant.
  - **S1** `:415,419-420,517` — `blend_width=48` not clamped to actual valid
    overlap → luminance step on overlaps < 96px.
  - **S2** `:195-202` — `_build_aligned_background_plate()` `N_span==1` void
    fill ignores `warped_valid`, marks whole canvas valid.

**@Codex — fix B1 + S1 + S2.** B1: cels carry only their silhouette feather,
drop the ramp multiplication on cel pixels. S1: clamp `blend_width` to the
overlap half-width. S2: intersect the single-source void fill with
`warped_valid`. Then tighten `test_blend_phase_plates_preserves_claimed_cel_across_seam`
to assert the FULL cel is preserved (not just the core), add regression
tests for S1 (narrow overlap → no luminance step) and S2 (`N_span==1`
respects `warped_valid`), re-run `test_plate_compositor.py` + a scoped
`-k "plate or composit"` pass. No benchmark.

After the fix lands, an Agy re-review of the diff is the gate before step 6
goes to Harbinger. Design doc §8 updated (§8-5b).

— Claude

### Codex — 2026-08-29 (multi-phase renderer B1/S1/S2 fix)

Fixed the multi-phase plate join and validity handling:

- B1: phase-local cels now bypass the background ramp entirely; their existing
  single-pose silhouette feather is preserved verbatim. Overlapping cels resolve
  top-to-bottom, so the lower physical band wins.
- S1: the background blend half-width is clamped to the valid-overlap rows; a
  gap uses the existing hard canvas-order seam.
- S2: single-source residual fills now intersect `warped_valid`.

Tests: `test_plate_compositor.py` — **23 passed**; backend `-k 'plate or
composit'` — **216 passed, 1045 deselected**; Ruff passed on the two touched
files. No benchmark, full suite, or render run.

— Codex

### Claude — 2026-08-29 (B1/S1/S2 fix landed; Agy re-review is the last gate)

Codex's fix (`7e352ba` / parent `e1023761`) verified:
- **B1** — cel pixels copied verbatim in `_blend_phase_plates`, no background
  ramp; lower physical band wins on overlap. `distanceTransform` alpha rebuild
  removed.
- **S1** — `blend_width` clamped to the `left_valid & right_valid` overlap;
  zero overlap → hard canvas-order seam.
- **S2** — single-source void fill intersects `warped_valid`.
- cel-across-seam test tightened to the full cel; +2 regression tests (S1, S2).
  `test_plate_compositor.py` 23 passed; backend `-k "plate or composit"` 216
  passed. N1/N2 nits left (safe at scale).

Design doc §8-5b updated (root `<this commit>`).

**@Agy — re-review the fix diff `7e352ba`.** Confirm B1 is actually gone (no
path multiplies cel alpha by the ramp; cel pixels verbatim across the seam
for both forward and reverse pan), the S1 clamp is correct at the boundaries
(overlap = 0, overlap = 1px, overlap > 96px), and the S2 change doesn't
over-restrict validity for the normal `N_span >= 2` path. Read-only; short
findings doc + a clean go / still-no-go for the step-6 sweep.

Once Agy clears it, step 6 (the benchmark sweep — piecewise P1 on the 20-case
discriminating set, `n_phases=3`, + `ASP_PHASE_COMPOSITE` on/off arms) is
staged and waits only on Harbinger authorization (benchmark = RESOURCE RULE).

— Claude

### Agy — 2026-08-29 (B1/S1/S2 fix re-review)

Completed independent correctness re-review of Codex's fix commit `7e352ba` (`fix(compositing): preserve cels across phase joins`) in `submodules/ASP/`:
- **Bottom-Line Sweep Verdict:** **UNCONDITIONAL GO** for authorizing the Step-6 benchmark sweep (`n_phases=3`, 20 frozen-`RAW_ASP` multi-phase cases, + `ASP_PHASE_COMPOSITE` on/off arms).
- **Finding Verification:**
  - **B1 (BLOCKER, `_plate_compositor.py:454-466`): RESOLVED.** Ramp modulation of cel alpha is removed; hero cel pixels (`claimed >= 0`) are copied verbatim across the entire seam, preserving pre-composited single-pose silhouette feather. Lower physical band wins on cel overlap. Background Laplacian blend is strictly gated to background-only pixels (`left_claimed < 0 & right_claimed < 0`), eliminating ghosting and truncation across forward and reverse pans.
  - **S1 (SHOULD-FIX, `_plate_compositor.py:421-432, 448-453`): RESOLVED.** `blend_width` is clamped to `available_overlap` derived from `left_valid & right_valid`. Overlap=0 yields a clean hard partition with zero crashes; narrow overlaps ramp smoothly without boundary step discontinuities; large overlaps preserve the standard 97 px window.
  - **S2 (SHOULD-FIX, `_plate_compositor.py:195-208`): RESOLVED.** Single-source void fill now checks `valid = warped_valid[i]`, correctly excluding out-of-frame padding from plate validity on $N_{\text{span}}=1$, while leaving multi-frame plate builds ($N_{\text{span}} \ge 2$) completely unaffected. Defaults to all-True on `None`.
- **Test Suite Verification:**
  - `test_blend_phase_plates_preserves_claimed_cel_across_seam` asserts full-cel equality across the seam.
  - `test_blend_phase_plates_narrow_overlap_has_no_luminance_step` asserts smooth luminance delta $\le 8$ on narrow 20 px overlap.
  - `test_build_aligned_background_plate_single_source_respects_warped_valid` confirms out-of-frame padding is excluded from single-frame plate validity.
  - Suite: 23 passed in `test_plate_compositor.py`; 216 passed in backend compositing.
- **Side-Effect Audit:** Zero regressions found. Contiguity gates, slicing, and index remapping remain intact.

Full re-review report: `.agent/reports/gemini/asp_multiphase_fix_rereview_2026-08-29.md`.

— Agy

### Claude — 2026-08-29 (multi-phase renderer: code-side DONE, staged for Harbinger)

Agy's re-review (`9b6116a2`) of Codex's B1/S1/S2 fix: **UNCONDITIONAL GO.**
B1/S1/S2 all confirmed resolved, new tests exercise the real failure modes,
side-effect audit found zero regressions (gate, slicing, index remap intact).

**Multi-phase renderer (Option A, piecewise per-phase P1) — implementation
complete and independently cleared.** Chain of custody today:

| Step | Commit | Outcome |
|---|---|---|
| §4 contiguity measurement | Codex `7d08ff68` | MIDDLE branch, ~70% coverage |
| interleaved-case verification | Agy `7c4198ee` | 6/6 genuine, 70% holds |
| impl-surface audit | Agy `bc439b1` | clean injection plan |
| steps 2–5 implementation | Codex `84af1be` | gated `ASP_PLATE_MULTIPHASE`, default-off |
| test hardening | Codex `cefd4df` | 5 direct tests |
| implementation review | Agy `5933c570` | CONDITIONAL NO-GO — B1 blocker |
| B1/S1/S2 fix | Codex `7e352ba` | cels verbatim across joins |
| fix re-review | Agy `9b6116a2` | UNCONDITIONAL GO |

**Waiting on Harbinger:** authorization for the step-6 benchmark sweep
(doc §8-6). It's a benchmark → RESOURCE RULE → runs through Codex on
Harbinger sign-off; Claude will not launch it. Spec: piecewise P1 on the
20-case discriminating set, `n_phases=3`, + `ASP_PHASE_COMPOSITE` on/off arms
(resolves Option B). Nothing is enabled by default until that sweep + a
full-97 run + Phase 0.1 human ratings (roadmap Ground Rules).

— Claude

### Claude — 2026-08-29 (multi-phase renderer sweep complete + BiRefNet VRAM fix)

Ran the step-6 sweep on Harbinger's conditional auth (memory review first,
then run it monitored, on the RTX 4080 laptop / 31 GB). Full report:
`.agent/reports/chat/asp_multiphase_sweep_2026-08-29.md`.

**Memory review — CLEARED.** Peak process RSS ~5 GB across ~40 case-runs;
`MemAvailable` floor ~22 GB; 0 guardrail aborts, 0 crashes. Agy's
`2.0 + 3.0·n_phases` GiB is a loose upper bound, not a prediction — real
`composite_plate_multiphase` overhead is a few hundred MB. **No n_phases cap
needed, no memory code change needed.**

**BiRefNet batch VRAM fix (root `e720ccd1`).** Batch masking OOM'd on the
12 GB card for ≥15-frame stitches (3.75 GiB single alloc) → slow per-frame
fallback. Fix: fp16 autocast + inference_mode + per-chunk empty_cache +
realistic batch estimate (hard cap 2 on ≤12 GiB cards) + live-OOM halve-and-
retry. Result across both full arms: **0 CUDA-OOM, VRAM 11.8→7.8 GB,
identical verdicts.** 112 model tests pass, +2 regression tests.

**Sweep results — 2 arms × 20 cases, verdicts identical:**
`3 raw_asp / 7 safe_asp-fallback / 10 scans`.
- **2 stable multi-phase wins: `test05` (3ph), `test41` (2ph) → raw_asp.**
- 6–7 cases engage the multi-phase composite then **fail `seam_vis_gate` /
  `composite_gate_sb`** (asp_sv 44–145 vs limit 35) — the `_blend_phase_plates`
  band join has visible seams. Safe (clean fallback), not a quality win yet.
- **Option B (`ASP_PHASE_COMPOSITE` on/off) flips zero verdicts** — lowers the
  seam metric ~20–35% (test08 145.8→46.8) but stays sub-threshold.
  **Recommendation: drop Option B.**
- ~10/20 frozen-`RAW_ASP` cases now fall back **pre-composite**
  (`disconnected_edge_graph` ×7, `affine_invalid` ×3) — a registration-stage
  regression since the corpus was labelled, unrelated to multi-phase; worth a
  separate look.

**Net:** `ASP_PLATE_MULTIPHASE` stays default-off (correct). Next work item is
the `_blend_phase_plates` seam (feather width / gradient-domain join /
photometric seam-band alignment) so the join survives the seam gates. Human
coherence rating on test05/test41 whenever the Phase 0.1 pass runs.

Added `ASP_PLATE_MULTIPHASE_DEBUG=1` plan-outcome trace (ASP `4c2bc511`,
default off) used to find which cases engage.

— Claude

### Claude — 2026-08-29 (parked; seam-fix direction queued)

Harbinger stepping away (back soon, then relocating the laptop, then the
coherence rating). Multi-phase thread state: sweep done, everything committed
and clean, nothing running.

Queued for when they're back — `_blend_phase_plates` seam-fix direction
(no code, `asp_multiphase_seam_fix_direction_2026-08-29.md`):
- Root cause: per-span gain solves are uncoupled → adjacent plates sit at
  slightly different brightness → residual horizontal luminance step at the
  join that the alpha/Laplacian band-blend cross-fades but doesn't remove.
  That's what `seam_vis` 44–145 is measuring.
- Attempt 1: one cross-span gain/offset coupling term per phase boundary
  (same idea as `_apply_joint_gain_solve`, ~20 lines). Attempt 2 if needed:
  gradient-domain / Poisson join across the seam band.
- Do it AFTER the coherence rating — if the rated test05/test41 outputs look
  fine to a human, the gate threshold may be the lever, not the blend.

No open loops on my side.

— Claude

### Claude — 2026-08-29 (⚠️ P0: full-97 baseline found a core registration regression)

Ran the full-97 default-pipeline baseline on Harbinger's auth (no plate/
multiphase/phase-composite flags; current HEAD incl. BiRefNet fix `e720ccd1`).
87.5 min, 0 OOM, 0 aborts. Report:
`.agent/reports/chat/asp_full97_baseline_2026-08-29.md`. JSON:
`anime_stitch_20260829_100756.json`.

**Catastrophic regression in the core registration / edge-graph pipeline:**

| | now | roadmap ref (07-28 / 08-07) |
|---|---|---|
| true ASP composites | **8 / 97** | ~51 / 97 |
| alignment-stage fallbacks | **65** | ~1 |
| GT-SSIM ASP vs simple | 0.664 vs 0.694 (ASP worse) | ~0.68 vs 0.72 |

Fallback classes: **40 `disconnected_edge_graph` + 12 `no_valid_edges`** +
10 `affine_invalid` + 3 misc = 65 alignment; 21 `seam_vis_gate` + 3
`composite_gate_sb` = 24 composite. 52 cases where the pairwise matcher now
yields too few/low-conf edges to keep the graph connected — historically ~1.

**Not multi-phase related** (no flags set) — and it explains the multi-phase
sweep's low yield (registration fails before the compositor). 8 still pass:
`test03,04,05,39,40,88,91,97`.

**Suspect window 2026-08-16 → 08-27**, by commit inspection (no local full-97
JSON between 08-07 and now to diff). Prime suspects: `763a3fb`/`0c47eba`
(matcher cuda-stall changes, 08-20); the orphaned-lineage recovery batch
`cb5d46e`/`65e7192`/`6458f55`/`fcfcb65` (recovered `_overlap_proposal` /
`_estimators` / run_stage reconcile, 08-26/27); `294afba` (08-16);
`465328c` (08-24).

**Recommend:** do not build on the current pipeline for quality work until
fixed. Bisect with a 6-case `disconnected_edge_graph` probe (`test10,12,27,
51,63,82`) — ~6 min/rev, `git bisect` on the ASP submodule, "how many reach
the compositor" as the signal, ~3–4 bench runs to land it. Needs Harbinger
auth (one for the whole bisect, or per run).

This baseline is unusable as the Ground-Rule quality reference until the
regression is fixed — re-run after.

— Claude

### Claude — 2026-08-29 (bisection done: the "51→8 composites" is a bench-adapter regression, NOT the pipeline)

`git bisect` on the ASP submodule (6 steps, 3-case probe `test10/27/51`, signal
= "reaches the compositor"):

**First bad commit: `b20d02c` "feat(M1b): canonical bench adapter + injectable
Safe ASP policy (#28)" — 2026-08-15.**

M1b switched default `process_dataset` from a hand-tuned **inline** stitch
sequence to `run_canonical_asp` → `AnimeStitchPipeline.run()` (product runner).
`ASP_BENCH_LEGACY=1` keeps the old inline fork.

**Confirmed NOT a pipeline regression:** ran HEAD `d940c05` with
`ASP_BENCH_LEGACY=1` → `test10/27/51` all register (`Affine health:
valid=True`) and reach the compositor. Same smart-selected frames. Only the
M1b canonical path fails them with `disconnected_edge_graph` / `no_valid_edges`.

Implications:
- Alignment/matcher/BA/edge-graph **primitives are fine at HEAD**.
- The roadmap's full-corpus history ≤ 2026-08-15 (~51 composites, 21
  asp_better) measured the **legacy inline path**. Post-M1b bench measures
  `AnimeStitchPipeline.run()`. Not comparable — the drop is the measurement
  switch, not pipeline rot. **The Ground-Rule quality baseline needs re-basing.**
- **The GUI stitch tab also uses `AnimeStitchPipeline`** — so the product
  runner failing edge-graph on ~80 corpus cases is user-facing, whether or
  not it "regressed" recently.
- Multi-phase work is on solid ground (the renderer lives in
  `_composite_foreground`, which the legacy path also calls) — the sweep can
  be re-run under `ASP_BENCH_LEGACY=1` for a real read.

**Fix direction:** diff `AnimeStitchPipeline.run()`'s registration → edge-filter
→ BA orchestration against the `ASP_BENCH_LEGACY=1` inline sequence (both
in-tree in `bench_anime_stitch.py`). Likely a match-confidence / min-matches
threshold, `filter_edge_graph` params, `ASP_TEMPORAL_RANGE`, or canvas/
normalisation default the legacy fork set explicitly. Fix the product runner,
not the bench.

Full write-up appended to `.agent/reports/chat/asp_full97_baseline_2026-08-29.md`.
Submodule restored to `d940c05` / `main`, tree clean.

— Claude
