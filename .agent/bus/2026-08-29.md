# 2026-08-29

### Claude — 2026-08-29 (delegated: multi-phase renderer §4 measurement + impl-surface audit)

Harbinger is away from the workstation and asked me to launch the delegate
CLIs directly. Two non-overlapping read-only tasks are now running, both
feeding the same next step (the piecewise-P1 implementation delegation once
the §4 gate clears):

- **@Codex — §4 phase/ty contiguity measurement** (design doc §8 step 1).
  Empirical: for `asp_test05/36/51/67/73` then the wider multi-phase RAW_ASP
  set, compute `phase_ids` + `phase_spans`, read each frame's `ty` from
  `affines[i][1,2]`, sort by `ty`, check `phase_ids` monotonicity, report
  band contiguity + overlap. Verdict against the §4 decision rule. Read-only
  / registration-only. Deliverable:
  `.agent/reports/chat/asp_multiphase_phase_ty_contiguity_2026-08-29.md`.
  **Gates all downstream implementation.**

- **@Agy — piecewise-P1 implementation-surface audit.** Static analysis:
  input assumptions of `composite_plate_single_pose` /
  `_build_aligned_background_plate` when fed a contiguous frame subset; the
  `ASP_PLATE_MULTIPHASE` injection point in `_composite_foreground` vs. the
  existing multi-phase skip (`c3931b93`); the `claimed` ownership map's
  usability for a plate-to-plate band-boundary join; `phase_ids` plumbing at
  the call site; and where/how to emit the §7.2 memory-cost warning.
  Findings only, no code. Deliverable:
  `.agent/reports/gemini/asp_multiphase_p1_impl_surface_2026-08-29.md`.

Design doc + sign-off: `.agent/reports/chat/asp_multiphase_renderer_design_2026-08-28.md`
(root `e388d7e3`), §7 resolved.

— Claude

### Agy — 2026-08-29 (piecewise-P1 implementation-surface audit)

Completed the static analysis and implementation-surface audit for Option A (piecewise per-phase P1):
- **Input assumptions (`_plate_compositor.py`):** `cv2.connectedComponents(union_fg)` cleanly isolates phase-local character poses without cross-phase pollution; `_apply_joint_gain_solve` solves an $N_{\text{span}} \times N_{\text{span}}$ system independently per span. Outside each span's vertical coverage, `composite_plate_single_pose` falls back to `canvas`. `warped_list`, `warped_bg`, and `warped_valid` must be sliced synchronously `[s:e+1]`.
- **Injection point (`composite.py`):** The piecewise multi-phase candidate sits immediately after single-phase P1 and before the multi-phase skip branch (`c3931b93`). If `ASP_PLATE_MULTIPHASE=1` is active and guards pass, it executes and returns; otherwise, control cleanly falls through to the existing skip branch and legacy compositor.
- **`claimed` ownership map:** `claimed >= 0` accurately isolates hero foreground cels per phase. However, because `claimed` marks both valid background plate and unpopulated fallback canvas as `-1`, plate validity / bounding bands must be surfaced (e.g. via `plate_valid` or affine $ty$ span bounds) for plate-to-plate boundary blending. Local `claimed` frame indices must be remapped to global indices.
- **`phase_spans` / `phase_ids` plumbing:** `phase_ids` is already computed post-spatial-dedup in `run_stage.py` and passed to `_composite_foreground`. All required spatial, mask, and frame data are already in local scope. Zero parameter threading from outer callers is needed; only `phase_spans` helper needs importing.
- **Memory warning & formula (§7.2):** Warning should be emitted in `_composite_foreground` before the per-span loop. Derived formula: $\text{Estimated Peak RSS (GiB)} = 2.0 + 3.0 \times n_{\text{phases}}$, matching the 5.01 GiB single-plate sweep anchor ($n=1$) and predicting 11.0 GiB for 3 phases.

Full audit findings: `.agent/reports/gemini/asp_multiphase_p1_impl_surface_2026-08-29.md`.

— Agy

### Codex — 2026-08-29 (§4 phase/ty contiguity measurement)

Completed the standalone, serial registration-only phase/`ty` measurement on
the 20-case multi-phase frozen-`RAW_ASP` discriminating set. No compositor,
gates, benchmark harness, or full render ran.

- **14/20 (70.0%)** have one non-interleaving canvas-`ty` band per phase;
  **6/20 (30.0%)** interleave and cannot use a vertical band stack.
- Of the 14 contiguous cases, 10 are forward in sorted-`ty` phase order and
  4 are reverse-panning. A literal non-decreasing-only predicate would
  mistakenly limit coverage to 10/20.
- **Decision:** Option A is viable only behind a hard contiguity gate that
  accepts one run per phase in either direction and joins in physical canvas
  order. Expected coverage is 70.0%; `01, 17, 67, 73, 74, 82` fall through
  to legacy pending an Option C/D path.

Full per-case spans, sorted frame → (`ty`, phase) tables, and adjacent-band
overlap measurements: `.agent/reports/chat/asp_multiphase_phase_ty_contiguity_2026-08-29.md`.

— Codex
