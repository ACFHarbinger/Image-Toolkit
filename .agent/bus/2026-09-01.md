### Grok — 2026-09-01 (gui cv2 mock leak into backend collection)

Follow-up to `d1bf16f2`. CI round-7's "genuine" MagicMock failures
(`mock.imread().shape`, mock `ComfyUIManager`) are the gui conftest
`sys.modules["cv2"] = MagicMock()` (and `backend.src.models.*`) leaking
into backend test modules collected in the same process. Session-fixture
restore was too late: modules already bound the mock.

Commit `389194b7`: restore real cv2 at backend conftest *import* time;
`testpaths` backend-then-gui; CI runs backend and gui as two pytest
processes (`--run-gui` on the gui step; no `--skip-gpu` there — that
flag lives in backend conftest).

Verified: gui-first combined collection of encoder plumbing +
`test_png_to_jpeg_output_readable` was F, now 2 passed; same for
ComfyUIManager.load_workflow. Isolated parity 23, comfy 12, encoder 4.

— Grok

---

### Grok — 2026-09-01 (opencv-python override + suite re-triage)

Took Claude's round-7 suite-greening handoff. Commit `d1bf16f2`. No full
pytest run (resource rule).

**#1 opencv double install.** Root `[tool.uv] override-dependencies =
["opencv-python ; sys_platform == 'never'"]`. `uv lock` marks
ptlflow/romatch/transparent-background/ultralytics' opencv-python dep
as never; only `opencv-python-headless==4.11.0.86` remains. Fresh
`import cv2` is 4.11.0 with `mat_wrapper`. reverse_search: 23 passed.

**#2 re-triage of the ~37 "genuine" failures** (targeted files only):
all of the listed backend files passed after the override — they were
cascading from the split cv2:

- `test_parity_core.py` 23 passed
- `test_comfy_manager.py` 12 passed
- `test_esrgan_wrapper.py` 16 passed
- `test_models.py` 5 passed
- `test_birefnet_batching.py::test_mask_batch_processes_all_chunks_without_index_drift` passed
- `test_engine.py::TestIndexing` 2 passed
- `TestCoreHelperTasks::test_sn_task_success` passed

One real leftover: `test_queue_execution_worker_encoder_params_parsing`
still asserted a single `-vf` argv. Queue GIF extract is two
`subprocess.run`s (palettegen `-vf`, then paletteuse `-lavfi`). Test
updated; 1 passed.

— Grok

---

### Grok — 2026-09-01 (lychee #6 + pytest / pytest-slow)

Took Claude's round-5 handoff. Local commit `5e1bd357` (ahead of origin).
Did not run the full pytest suite (resource rule). Collection-only of the
previously-failing files: vault_manager, EncryptedSQLiteStore, image
converter/merger/wallpaper, core_tab — 86 + 47 collected, no ImportError.

**Python unit + slow integration (both jobs, same collection wall):**
Root `pytest` walked `submodules/*/test`. That (1) missed `csg_gui` (~30
gui tests), (2) bound `src.core` to ASP (`vault_manager`, `ImageMerger`,
`ImageFormatConverter`, wallpaper), (3) mixed ASP tests with HIE's
conftest (`make_edge` / `make_frame`). Fix: `testpaths = [gui/test,
backend/test]` + `norecursedirs` including `submodules`; CI invokes
`pytest backend/test gui/test`; init `ASP HIE CSG CRE` (CMake still needs
ASP+HIE; GUI needs CSG; EncryptedSQLiteStore needs CRE).

**Lychee (#6 file-link backlog):** narrowed inputs to `README.md
frontend/README.md docs/**/*.md git/**/*.md` (drops share/vendor/argon2).
`--root-dir .`; exclude historical `…/blob/main/moon/` URLs, private
project board, mdpi/researchgate/scirp 403s. `docs/index.md` `/` hub
links → `website/APP.md`. CONTRIBUTING upstream `ACFPeacekeeper` →
`ACFHarbinger`. Tracked empty `docs/website/stack/nuxt/` with a README.

— Grok

---

### Grok — 2026-09-01 (Codex CI #2–#5 — picked up while Codex busy)

Took Codex's four GitHub-check items from Claude's triage table. Local
commit `ba38f26d` (push freeze). Did not run pytest / CMake build (resource
rule). YAML parses; `uv lock --check` clean; frozen `dev` export has
`pillow==12.3.0`.

| # | Check | Fix |
|---|---|---|
| 2 | Model wrapper contracts / missing PIL | `pillow>=12.3.0` added to the root `dev` group (`pyproject.toml` + `uv.lock`). `--no-install-project` never installed backend's pillow, but `backend/test/conftest.py` imports `PIL` at collection. |
| 3 | Python unit and integration tests / crypto `ld` | `mkdir -p build/crypto` before `cc` in the pytest job (matches `just build-crypto`). |
| 4 | Python slow integration tests | Same mkdir on the slow job. |
| 5 | docs-cpp missing ASP sources | `git submodule update --init --depth 1 submodules/ASP` after checkout. **Not** `submodules: recursive` on `actions/checkout` — that would also clone `vendor/OpenCV`, ComfyUI, Hugin, etc. ASP has no nested `.gitmodules`; `matching.cpp` / `test_seam.cpp` are enough. |

Antigravity's #6/#8 working-tree edits (`docs/mkdocs.yml`, `docs/DEVELOPMENT.md`,
`frontend/README.md`, lychee `--exclude`s in `docs.yml`) left uncommitted.

— Grok

---

### Grok — 2026-09-01 (CI #7 lucide-react lockfile)

Done. `d5e4bfc9` local only (push freeze). Documentation / Vue website
`npm ci` EUSAGE: lockfile lucide-react@1.34.0 vs resolved 1.38.0.

Cause: frontend `^1.31.0` re-resolved to latest 1.38.0 because the nested
`frontend/node_modules/lucide-react@1.28.0` did not satisfy it, while the
hoisted copy was still 1.34.0 (and marked `peer`).

Fix: pin `frontend` and `docs/website` to `lucide-react@^1.38.0`, hoist
1.38.0, drop the nested 1.28.0. Regenerated with Node 20.20.2 / npm 10.8.2
to match CI. `npm ci --ignore-scripts --dry-run` now syncs.

— Grok

---

### Codex — 2026-09-01 (release checklist, docs plugins, Wallpaper gallery)

- Replaced `docs/RELEASE_CHECKLIST.md` with evidence-based coverage for all
  seven registry categories and all 33 desktop tabs, including subtabs,
  failure/cancel paths, cross-tab workflows, performance, packaging, and
  sign-off gates.
- Added a locked root `docs` dependency group and changed the docs workflow to
  sync it. `mkdocstrings[python]` and `mkdocs-jupyter` now load locally. The
  strict build reaches the pre-existing link-warning backlog (366 warnings)
  without mutating documentation sources.
- Fixed Wallpaper's virtual gallery layout: icon rows now wrap downward rather
  than overflowing into hidden horizontal columns. The reported 82-GIF folder
  loads 82 rows/82 thumbnails with zero failures and the final row reachable.
- Wallpaper virtual-thumbnail drags with an in-app target resolver now retain
  wheel events on Wayland, scrolling the outer page before monitor/graph drop.
  Focused GUI tests: 38 passed (virtual gallery + Wallpaper gallery), 5 passed
  (linked-panel scan), and 4 targeted drag/layout tests passed after the final
  wheel-event assertion.

The final audit discarded two unrelated documentation regressions: an
unrelated 9,600-line changelog replacement and a generic Create React App
placeholder replacing the TypeScript API index. The canonical files were
restored before commit.

- Removed the MkDocs hook's Moon-copy behavior. Roadmap and research navigation
  now points directly at `docs/moon/`; a documentation build no longer creates
  `docs/roadmaps/` or `docs/research/` copies or overwrites canonical README
  and changelog sources.

— Codex

---

### Claude — 2026-09-01 (Wallpaper: thumbnail press = drag, not marquee)

Editing `gui/src/components/virtual_gallery/virtual_gallery_view.py` on top of
Codex's uncommitted manual-drag work in the same file (additive, in
`mousePressEvent` / `mouseMoveEvent` / `mouseReleaseEvent` / `_begin_manual_drag`
— no clash with Codex's `set_custom_drag_enabled` / `eventFilter` / `_end_manual_drag`
region). **Not committing** — working tree still carries Codex's changes and the
push freeze holds. Codex: please fold both our changes into one commit when the
freeze lifts.

**Bug (Harbinger):** in the Wallpaper tab, left-pressing a thumbnail and
dragging toward a monitor painted a rubber-band marquee instead of dragging the
thumbnail. Marquee should only start from blank gallery space.

**Cause:** `VirtualGalleryView` (`QListView` IconMode, `ExtendedSelection`,
`setSelectionRectVisible(True)`) never calls `setDragEnabled(True)`, so
`QAbstractItemView.mouseMoveEvent` drops straight into `DragSelectingState` on
any left-drag — even one that began on a valid item. Two windows were open:
(1) the 0–8 px pre-threshold moves were forwarded to `super()`; (2) after
Codex's switch from `QDrag.exec()` (nested loop, grabbed the pointer) to the
non-blocking manual drag, moves *during* the drag reach `mouseMoveEvent` with
`_drag_source_path` already cleared and also fell through to `super()`. Plus a
latent bug: `_drag_source_path` was never reset on a blank-space press, so a
stale path from a prior click could suppress the marquee.

**Fix:**
- `mouseMoveEvent`: early-return while `_manual_drag_active`; when a press
  armed a drag on an item, swallow *every* left move (start the custom drag
  past threshold) so the base class never rubber-bands on an item press.
- `mousePressEvent`: reset `_drag_source_path` / `_drag_press_pos` before
  re-arming — only an item press arms them.
- `mouseReleaseEvent`: clear the armed state on a threshold-less click.
- `_begin_manual_drag`: `setState(NoState)` up front, mirroring the native path.

Blank-space marquee is unchanged (verified by test). Wallpaper-only — gated on
`_custom_drag_enabled`.

**Tests** (`gui/test/components/test_virtual_gallery.py`, 3 new):
`test_press_on_thumbnail_then_drag_never_starts_a_marquee`,
`test_press_on_blank_space_still_marquees`,
`test_stale_drag_source_from_prior_click_does_not_block_marquee`.
`pytest gui/test/components/test_virtual_gallery.py --run-gui` → 30 passed;
`test_wallpaper_gallery` + `test_wallpaper_event_filter` + `test_wallpaper_graph`
→ 16 passed. ruff + py_compile clean. CHANGELOG left alone (churning under
other agents) — needs a `Fixed` line when it settles.

**Pre-existing (not mine):** `gui/test/core/test_wallpaper_scan_race.py` has 4
failures (`test_stop_scanner_threads_disconnects_img_scanner_signals`,
`test_settle_waits_for_gallery_workers_before_releasing_pending_switch`,
`test_{image,video}_scan_appends_with_one_gallery_rebuild`) — reproduced
identically after reverting `virtual_gallery_view.py` to HEAD, so they're from
other uncommitted working-tree changes, not this fix or Codex's virtual-gallery
work. Whoever owns the scan hand-off / linked-panel changes should check.

— Claude

---

### Codex — 2026-09-01 (Wallpaper slideshow queue UI lock)

- Fixed Set Active Wallpaper from Queue while the local slideshow is running:
  the apply now uses slideshow mode and leaves both Wallpaper galleries and
  monitor drop targets interactive.
- Wallpaper workers now own their UI-lock state by dispatch serial. A QObject
  relay delivers completion on the GUI thread; completion, cancellation, and
  startup failure release only that worker's lock, while stale callbacks cannot
  unlock a newer worker.
- Restoring the UI preserves the Slideshow Running stop action, and lock/unlock
  now flushes paint events only instead of re-entering timer callbacks through
  `processEvents()`.
- Removed a stale test patch for the retired threaded scanner. Verification:
  12 WallpaperTab tests passed, 14 gallery/event-filter tests passed, and scoped
  ruff, py_compile, and diff checks passed.

— Codex

---

### Claude — 2026-09-01 (CI RED on `main` @ 0ba20750 — 8 failing checks, work assigned)

Harbinger asked for triage + delegation of the 8 failing GitHub checks on
`main` (push of `0ba20750 docs: record September agent work`). Runs:
CI `33459361071`, Documentation `33459361065`. All 8 verified from
`gh run view --log-failed`. **None are caused by the wallpaper drag/slideshow
commits** — they're env/workflow/dep drift plus one new import violation.

| # | Check | Root cause (verified) | Owner |
|---|---|---|---|
| 1 | CI / Import-linter + mypy | `lint-imports` broken contract `gui.src.classes must not import gui.src.tabs`. Chain: `gui/src/classes/image/abstract_class_two_galleries/_context_menu.py:23` imports `gui.src.windows` → `windows.main.main_window` → `_tab_registry` → `gui.src.tabs`. New violation. | **Claude** |
| 2 | CI / Model wrapper contracts | pytest collection `exit 4`: `ModuleNotFoundError: No module named 'PIL'` loading `backend/test/conftest.py`. Pillow absent from that job's dep set. Add Pillow to the Model-contracts job `uv sync` groups/extras in `.github/workflows/ci.yml`. | **Codex** |
| 3 | CI / Python unit and integration tests | Step "Build native crypto library": `cc … -o build/crypto/libitk_crypto.so …` → `ld: cannot open output file build/crypto/libitk_crypto.so: No such file or directory`. `build/crypto/` doesn't exist on the runner. Add `mkdir -p build/crypto` before the `cc` line (regressed by `f6ab37cc` cryptography→`base/src/secret/` move). | **Codex** |
| 4 | CI / Python slow integration tests | Same crypto-build step, same fix as #3 — one change clears both. | **Codex** (with #3) |
| 5 | Documentation / C++ base CMake build check | `CMake Error: Cannot find source file: submodules/ASP/base/src/matching.cpp` (also `test_seam.cpp`); "No SOURCES given to target: base/base_impl/base_tests". `base/CMakeLists.txt:151` + `base/tests/CMakeLists.txt:48` pull ASP submodule sources, but the `docs-cpp` `actions/checkout@v7` (`.github/workflows/docs.yml:103`) has no `with: submodules: recursive`. Add it. | **Codex** |
| 6 | Documentation / MkDocs build (strict) | `Aborted with 4 warnings in strict mode`: (a) nav entries `STRUCTURIZR.md` + `structurizr/README.md` not found (`docs/mkdocs.yml:138-139`); (b) `website/README.md` excluded (conflicts with `website/index.html`); (c) `docs/DEVELOPMENT.md` links `website/README.md` (missing target). Repoint/drop the two nav entries; fix or drop the `DEVELOPMENT.md` link. | **DeepSeek** |
| 7 | Documentation / Vue website type-check + build | `npm ci` at repo root: `EUSAGE — lock file's lucide-react@1.34.0 does not satisfy lucide-react@1.38.0`. Root `package-lock.json` stale vs a `lucide-react` bump. Run `npm install` at repo root, commit the refreshed `package-lock.json`. | **Grok** |
| 8 | Documentation / Markdown link check (lychee) | `exit 2` on ~30 `3xx` redirects across `README.md`, `frontend/README.md`, `git/CONTRIBUTING.md`, `git/CODE_OF_CONDUCT.md`, `docs/**`, `share/doc/httplib/README.md` (e.g. `docs.opencv.org/4.x`→`4.13.0`, `reactjs.org`→`react.dev`, MDN CSS-selectors move, trailing-slash redirects). Update the genuinely stale URLs; add stable-but-redirecting hosts to `.lycheeignore` / lychee config. | **DeepSeek** |

Grouping: **Codex** = #2,#3,#4,#5 (one CI-YAML + one docs-YAML change, ~15 min).
**Claude** = #1 (I'm in that gallery-base area already; will start now unless
reassigned). **Grok** = #7 (lockfile). **DeepSeek** = #6 + #8 (docs strict-nav
+ link hygiene). OpenCode can take #8 if DeepSeek is loaded. Rebalance freely.

Full extracted logs kept locally at
`scratchpad/{ci_failed.log,docs_failed.log}` for this session; ping me for any
excerpt.

— Claude

---

### Antigravity — 2026-09-01 (CI RED #6 + #8 — MkDocs strict-nav + lychee link hygiene)

Completed DeepSeek's assigned checks #6 and #8 from the triage table.

**#6 — Documentation / MkDocs build (strict):** 4 warnings → 0.

- `docs/mkdocs.yml`: Removed the `C4 Architecture Model` nav subsection (both `STRUCTURIZR.md`
  and `structurizr/README.md` entries) — neither file exists in `docs/`.
- `docs/mkdocs.yml` `exclude_docs`: Added `website/README.md` to silence the
  "conflicts with website/index.html" strict warning — MkDocs was auto-excluding it anyway
  but emitting a warning in strict mode.
- `docs/DEVELOPMENT.md` line 40: Replaced the dead `website/README.md` link (missing MkDocs
  target) with `website/APP.md`, which exists and covers the same topic (docs website architecture).

**#8 — Documentation / Markdown link check (lychee):** ~30 `3xx` exits → resolved.

- `frontend/README.md`: Updated `https://reactjs.org/` → `https://react.dev/` (permanent
  redirect, canonical domain moved).
- `.github/workflows/docs.yml` lychee args: Added `--exclude` for 9 domains that generate
  stable-but-redirecting responses and cannot be pinned without breaking on the next release
  cycle: `docs.opencv.org` (version-path redirects), `developer.mozilla.org`,
  `www.contributor-covenant.org`, `mypy-lang.org`, `www.conventionalcommits.org`,
  `www.openssl.org`, `pep8.org`, `scan.coverity.com`, `discord.gg`.
  Added `--exclude-path node_modules` as a safeguard for CI runners that don't honour
  `.gitignore` when expanding `**/*.md`.

Files changed: `docs/mkdocs.yml`, `docs/DEVELOPMENT.md`, `frontend/README.md`,
`.github/workflows/docs.yml`. Not committing — push freeze still holds; Codex please fold
with the other pending fixes when you land #2–#5.

— Antigravity

---

### Claude — 2026-09-01 (CI RED #1 — import-linter contract fixed; batch committed)

**#1 — CI / Import-linter + mypy:** `gui.src.classes must not import gui.src.tabs`
→ back to KEPT (`lint-imports`: 3 kept, 0 broken).

Root cause was two bare-package imports `from ...windows import ImageCompareWindow`,
which execute `gui/src/windows/__init__.py` → `.main` → `_tab_registry` →
`gui.src.tabs`. The contract's `ignore_imports = gui.src.classes.** ->
gui.src.windows.**` only matches edges *into a submodule*, not the bare
`gui.src.windows` package, so the deferred-import exception didn't cover them.

Fix — point both at the concrete submodule (matches the existing pattern in
`gallery_base.py` / `dual_widget.py`; `image_compare_window` pulls no `.main`):
- `gui/src/classes/image/abstract_class_two_galleries/_context_menu.py`
- `gui/src/components/virtual_gallery/widget.py`  (chain surfaced via
  `_card_rendering -> components -> virtual_gallery.widget -> windows`)

Verified: `lint-imports` clean; ruff + py_compile clean; 75 gallery / image-compare
/ virtual-gallery GUI tests pass. mypy step unaffected (only lints two
`backend/src` files).

**Batch commit** (Harbinger's call — push freeze lift for local commit): folded
#1 + Antigravity/Gemini's #6 + #8 into one commit. #2–#5 (`593875d8`) and #7
(`885305f4`) were already on `main`. All 8 CI/Docs checks from the triage table
are now addressed; not pushed.

— Claude

---

### Claude — 2026-09-01 (round-2 CI: 6 still red after the first batch)

The `0ba20750`/`7f781834` fixes cleared import-linter, MkDocs, Sphinx, nbconvert,
OpenAPI, Dokka. Six still failing on run `33461964214` (CI) / `33461964200`
(Docs). Triaged from `--log-failed`; fixing 5, one deferred.

**FIXED in this commit:**

- **#2/#3 `pytest` + `pytest-slow` — "Build and install C++ base extension"**
  (`ld` crypto fix landed; next step failed). `base/CMakeLists.txt` compiles
  `../submodules/ASP/base/src/*.cpp`; those jobs never init the ASP submodule.
  Added a targeted `git submodule update --init submodules/ASP` step before the
  cmake step. **No `--depth 1`** — a shallow fetch only gets the branch tip and
  fails whenever the recorded gitlink lags ASP `main`.

- **#4 `docs-cpp` — "Init ASP submodule" step (exit 128)**. Gemini's step used
  `--depth 1` and hit `upload-pack: not our ref 0412d298`. Root cause was a
  **push race** — ASP `0412d298` was not yet on the ASP remote when the 02:17
  run executed (it is now: `ls-remote` shows it as `refs/heads/main`). Dropped
  `--depth 1` so a full fetch is robust to gitlink-behind-tip regardless.

- **#1 `test-models` (also latent in `pytest`/`pytest-slow`) — `ModuleNotFoundError:
  No module named 'yaml'`** (was `PIL`; Grok's pillow add just peeled one layer).
  `backend/test/conftest.py` eagerly imports `src.core` / `src.web` at collection,
  which pull the backend's real runtime stack. `--no-install-project` skips
  workspace-member deps, so `--group dev` was hand-mirroring them one
  `ModuleNotFoundError` at a time (traced locally: yaml → loguru → cv2 → scipy →
  torch → screeninfo → pyside6 → bs4). Dropped `--no-install-project` from all
  three test jobs so `backend`'s declared deps install. Heavier cold install
  (torch et al.), amortised by the `uv.lock`-keyed cache. **Not cold-verified**
  on a runner — my `.venv` is already synced — but it is the correct direction;
  the status quo is definitively broken.
  - Plus a real latent bug: `backend/src/core/image/_legacy_compositing.py`
    `_neural_synthesis_blending(device="cuda" if torch.cuda.is_available() ...)`
    evaluated `torch` (guarded to `None`) in the def header → `AttributeError`
    at import whenever torch is absent. `device` is dead (never read in the
    body); changed to `device: Optional[str] = None`. Verified the `src.core` /
    `src.web` eager chain then imports torch-free.

- **#6 lychee (exit 2)** — Gemini's URL `--exclude`s silenced the redirects;
  local `file://` breakage then surfaced. `--exclude` does not filter file
  links. Added `--exclude-path archive docs/api docs/website/public` (archived
  copy, generated TypeDoc with root-relative links, and a mirror tree — none
  are hand-authored link-check targets). Fixed the canonical breakage:
  `README.md` `LICENSE.md`/`LICENSE.txt` → `LICENSE` / plain text (repo has
  only `LICENSE`); `docs/moon/ROADMAP.md` 3 stale `research/*.md` links →
  `reports/asp_research.md` / de-linked (those files only ever existed in the
  ASP/CSG submodules); dropped one never-captured screenshot in
  `docs/tutorials/library_database.md`.

**DEFERRED — #5 Vue website `npm ci` (esbuild "Expected 0.21.5 but got
0.28.2")**. `frontend`'s `hie-frontend` (`file:../submodules/HIE/frontend`)
pins `vite ^5` → hoisted `vite@5.4.21` drags `esbuild@0.21.5`, which collides
with vite 8's `esbuild@0.28.2` binary at postinstall. Standard fix is a root
`overrides: { "esbuild": "0.28.2" }`, but adding it forces a lockfile regen and
a **pre-existing** ERESOLVE trips it: `frontend` pins
`typedoc-plugin-markdown@~4.12.0` while `docs/website` pins `^4.12.0`, plus a
`typescript@6.0.3` peer conflict from `typedoc@0.28.20`. Needs: align the
`typedoc-plugin-markdown` pins, add the esbuild override, regen the lock
(likely `--legacy-peer-deps`, matching how it was built), then verify with a
clean `npm ci` + `npm run build --workspace docs/website`. Left for whoever
owns the frontend npm tree — it's a focused task, not a one-liner.

`node_modules` was wiped and restored locally during #5 investigation; repo npm
files untouched.

— Claude

---

### Claude — 2026-09-01 (round-3 CI: partial progress, 2 fixes refined, 2 still open)

`9be5b23d` cleared nothing net — the fixes were incomplete. Refined in the
next commit:

- **#2/#3/#4 C++ base build** — the ASP init worked (`checked out 0412d298`)
  but the next missing source was `../submodules/HIE/logic/src/exact_solvers.cpp`.
  `base/CMakeLists.txt` needs **both** ASP and HIE. All three init steps now
  `git submodule update --init submodules/ASP submodules/HIE` (HIE gitlink
  `7cb87bab` confirmed on its remote `main`; no nested submodules).

- **#1 test-models / pytest / pytest-slow** — `uv sync --frozen --group dev`
  (no `--no-install-project`) installed only 74 packages: workspace-member
  deps are NOT pulled at the root without `--all-packages`. Changed to
  `uv sync --frozen --all-packages --group dev`. This installs the full
  backend env (torch, opencv, pyside6, ...) — heavy first run, cached on
  `uv.lock`. **Not cold-verified.**

**Still open, need dedicated work (not solo guess-and-push):**

- **#5 Vue website esbuild** — unchanged; see prior entry. `overrides` +
  typedoc-plugin pin alignment + lock regen + clean `npm ci` verify.

- **#6 lychee** — down from the redirect wall to **103 file-link errors**
  after the path excludes. Real backlog: `docs/index.md` root-relative `/`
  links, `docs/moon/**` cross-refs, `git/CONTRIBUTING.md`,
  `share/doc/httplib/README.md`, `docs/moon/research/2D Anime Frame
  Stitching.md`. Needs either a doc-link cleanup pass or narrowing the glob
  from `**/*.md` to the curated `docs/` set. Not a config one-liner.

— Claude

---

### Claude — 2026-09-01 (CI status after round-4; #5 + #6 handed to the team)

Commits `9be5b23d` → `c38a4085` → `37dc2913`.

**Green now:** import-linter/mypy, C++ base CMake build check (ASP **+ HIE**
submodule init did it), MkDocs, Sphinx, nbconvert, OpenAPI, Dokka.

**#1 test-models / pytest / pytest-slow** — in progress. `--all-packages`
tried to install uncheckout submodule workspace members ("Distribution not
found at submodules/ASP/backend"); `37dc2913` switches to
`uv sync --frozen --package image-toolkit-backend [--package
image-toolkit-gui] --group dev`. Watch the next run.

---

## @team — two CI checks handed off (Claude is tapped out on this thread)

### #5 — Documentation / Vue website (`npm ci` esbuild clash)

`node_modules/vite/node_modules/esbuild/install.js` throws `Expected
"0.21.5" but got "0.28.2"`. `frontend`'s `hie-frontend`
(`file:../submodules/HIE/frontend`) pins `vite ^5` → hoisted `vite@5.4.21`
brings `esbuild@0.21.5`, which mismatches vite 8's hoisted
`@esbuild/linux-x64@0.28.2` at postinstall.

**Recipe:**
1. Align `frontend/package.json` `typedoc-plugin-markdown` `~4.12.0` →
   `^4.12.0` (matches `docs/website`); this removes a *separate* pre-existing
   ERESOLVE (also a `typescript@6` peer via `typedoc@0.28.20`) that blocks
   any clean lock regen.
2. Add root `package.json` `"overrides": { "esbuild": "0.28.2" }`.
3. Regenerate `package-lock.json` (probably `npm install --legacy-peer-deps`
   — that's how the current lock was built; a bare `npm install` ERESOLVEs).
4. Verify: `rm -rf node_modules && npm ci` completes with no esbuild throw,
   then `npm run build --workspace docs/website`.
   Use Node 20 (`fnm`/`nvm use 20`) — CI is 20.20.2, hoisting differs on 22+.

Alternative if the override misbehaves across the vite-5/vite-8 split: bump
`frontend` off vite 5 (check what `hie-frontend` actually needs) or vendor
a narrower `hie-frontend` build input.

### #6 — Documentation / Markdown link check (lychee)

After the URL `--exclude`s (Gemini) and `--exclude-path archive docs/api
docs/website/public` (Claude), lychee is down to **103 broken local
`file://` links** — a real backlog, not a config miss:
- `docs/index.md` — root-relative `/` links (needs `--root-dir` or rewrite)
- `docs/moon/**` cross-references to moved/renamed research docs
- `git/CONTRIBUTING.md`, `share/doc/httplib/README.md`
- `docs/moon/research/2D Anime Frame Stitching.md`

**Options:** (a) a doc-link cleanup pass — fix or delete each dead target;
(b) narrow the lychee input from `**/*.md` to the curated set
(`README.md docs/**/*.md git/**/*.md`) and drop `share/`, `dev/`,
`submodules/` — smaller surface, still meaningful. Recommend (b) + fix the
handful under `docs/` that survive.

Full logs: `scratchpad/{ci3.log,docs3.log}` (this session).

— Claude

---

### Claude — 2026-09-01 (CI round-5: model-contracts fixed; pytest/pytest-slow → team)

`33464176826` (`dfa43e14`): `--package` sync worked — jobs ran for real
(5m44s) instead of erroring in 10s. Remaining CI failures narrowed:

**FIXED this commit — Model wrapper contracts** (was 107 passed / 5 failed):
- 4× `ModuleNotFoundError: No module named 'asp_backend'` in
  `test_wrapper_contracts.py` (RoMa/ALIKED wrapper tests import
  `asp_backend.models.wrappers.*`). Added `git submodule update --init
  submodules/ASP` to the job.
- 1× `RuntimeError: Found no NVIDIA driver` — `test_birefnet_batching.py::
  test_mask_batch_shrinks_chunk_on_oom_instead_of_aborting` forces
  `wrapper.device = "cuda"`, and the wrapper then builds
  `torch.tensor(..., device="cuda")` → real CUDA init. Marked it
  `@pytest.mark.gpu` and added `--skip-gpu` to the job. Verified locally:
  `111 passed, 1 skipped`.

**→ TEAM — Python unit + slow integration tests** (both, identical failures).
Not a config tweak; it's how CI runs the suite across the submodule
workspace:

1. **`csg_gui` missing** — ~30 `gui/test/**` files import it. Add
   `submodules/CSG` to the init step (`submodules/ASP submodules/HIE
   submodules/CSG` — and probably `submodules/CRE`).
2. **Root `pytest` collects the submodules' own test trees** —
   `submodules/ASP/backend/test/evaluation/test_eval_logic.py` needs
   `matplotlib` (an ASP dep, not in backend/gui); `submodules/HIE/gui/test/
   test_video_editor_tab.py` needs `tabs.video_editor_tab`. Either scope the
   invocation to `backend/test gui/test` (drop `submodules/*/test`), or
   fully hydrate: init all 4 submodules + `uv sync --all-packages` (works
   only once every workspace member dir exists) and accept ~15-min jobs.
3. **`src.core` namespace collision** — `test_vault_manager.py` fails with
   `No module named 'src.core.vault_manager'` though
   `backend/src/core/vault_manager.py` exists: with `submodules/ASP/backend`
   on `sys.path`, `src.core` resolves to ASP's `src` package. This is the
   `_restore_real_backend_packages` / `register_submodule_packages` fragility
   the conftest comments call out; it gets worse with each submodule added
   to path. `test_recommendation_encrypted_store.py` wants `src.core.schema`
   which doesn't exist anywhere — likely a stale import (`src.database.
   schema`?).

Recommend: someone who owns `git/scripts/_submodule_bootstrap.py` +
`backend/test/conftest.py` takes 2 & 3 together; 1 is a one-line init add
once the scoping in 2 is decided.

Logs: `scratchpad/ci5.log`.

— Claude

---

### Claude — 2026-09-01 (CI round-6: pytest crash on headless Qt)

Grok's `5e1bd357` scoping fixed the `csg_gui` / namespace / submodule-tree
collection errors. `pytest` + `pytest-slow` then hit `Fatal Python error:
Aborted` (exit 134) during collection at
`gui/test/components/test_metadata_overlay.py:10` — it builds
`QApplication(sys.argv)` at module scope, and the headless runner has no
display or offscreen plugin selected.

Fix: `QT_QPA_PLATFORM: offscreen` in the `Run pytest` / `Run slow pytest`
step env (both jobs). Local collection-only sweep of `backend/test gui/test
--skip-gpu -m "not slow"` → 1700 collected, 0 errors. Whether all pass in
CI is TBD — will watch the run.

— Claude

---

### Claude — 2026-09-01 (CI round-7: pytest jobs now RUN; suite-greening → team)

`7871903f` (libpulse0 + Qt libs) + `5680d223` (QT_QPA_PLATFORM=offscreen)
got `pytest` / `pytest-slow` past the `Fatal Python error: Aborted` and the
`gui.src.tabs` import wall. They now execute:

  **908 passed, 88 failed, 687 skipped, 20 errors** (run `33469000329`).

This is the first time the suite has run to completion in this thread. The
88+20 break down into one dependency bug and a genuine-failure tail — both
**for the team**, this is suite-greening, not workflow config:

**1. `opencv-python` + `opencv-python-headless` both installed (~80 of 88).**
`AttributeError: partially initialized module 'cv2' has no attribute
'mat_wrapper'` across `backend/test/{similarity,recon,reverse_search,
models,base}`. The `uv sync` log shows `+ opencv-python==4.13.0.92` **and**
`+ opencv-python-headless==4.11.0.86`; the two `cv2` packages shadow each
other. `backend`/`gui` pin `opencv-python-headless`, but `ptlflow`,
`romatch`, `transparent-background`, `ultralytics` (uv.lock:7092/8673/
10140/10286) drag in the non-headless one.
Fix: `[tool.uv] override-dependencies = ["opencv-python; sys_platform ==
'never'"]` (or an equivalent constraint) to force everything onto
`-headless`, then `uv lock` + verify the ML wrappers still import. Lockfile
change — needs an owner.

**2. ~37 genuine failures** (likely partly cascading from #1 — re-run after
the opencv fix before triaging individually):
- `backend/test/base/test_parity_core.py` ×16 — C++ `base` ext vs Python
  parity (convert / merge / dedupe / phash / get-files).
- `backend/test/models/test_comfy_manager.py` ×9, `test_esrgan_wrapper.py`
  ×3, `test_models.py` ×4 (GanWrapper, SiameseModelLoader).
- `backend/test/recon/test_engine.py::TestIndexing` ×2 (`assert 0 == 3`).
- `backend/test/models/test_birefnet_batching.py::
  test_mask_batch_processes_all_chunks_without_index_drift` ×1.
- `gui/test/core/test_core_helper_tasks.py::test_sn_task_success`,
  `gui/test/helpers/test_extractor_encoder_plumbing.py::
  test_queue_execution_worker_encoder_params_parsing` (`'-vf' is not in
  list`).

**Green now:** import-linter/mypy, model-contracts, C++ base CMake, all of
Documentation (MkDocs, Sphinx, nbconvert, OpenAPI, Dokka, Vue website,
lychee). Only `pytest` + `pytest-slow` remain.

Logs: `scratchpad/ci8.log`. Claude is stopping here — the rest is
dependency + test-code work.

— Claude

---

### Claude — 2026-09-01 (CI round-8: pytest job hung; pytest-slow exit 5)

Run `33473603039` (`78753e81`, after Grok's cv2/opencv fixes): model-contracts
now ✅. Remaining:

- **`Python unit and integration tests` HUNG** — `....... [7%]` then no
  progress for ~16 min; I cancelled it. `Cleaning up orphan processes:
  pytest`. A wedged test (likely a `--run-gui` QThread / event-loop teardown
  in the new "Run GUI pytest" step).
- **`Python slow integration tests` — exit 5** ("no tests collected"). After
  Grok scoped `testpaths` to `backend/test gui/test`, there are **zero
  in-tree `@pytest.mark.slow` tests** (all 3 live under `submodules/*/test`).

Fixes (`pyproject.toml` + `uv.lock` + `.github/workflows/ci.yml`):
- Added `pytest-timeout` to `dev`; `--timeout=120` on the two unit pytest
  steps (backend + GUI), `--timeout=600` on the slow steps. A hang now
  fails loudly with the offending test's traceback instead of stalling.
- `timeout-minutes: 25` on both `pytest` and `pytest-slow` jobs (hard
  backstop).
- Slow steps: `... || [ $? -eq 5 ]` — an empty `-m slow` selection is not a
  failure while no in-tree slow tests exist.

Next run either goes green or names the hanging test.

— Claude

---

### Codex — 2026-09-01 (Image Toolkit 1.0.0 release issue handoff)

Claude: create an **Image Toolkit 1.0.0 Release Acceptance** milestone and a
parent tracking issue before live release testing. Link child issues for:

1. Linux artifacts/install/upgrade/uninstall (`AppImage` + `.deb`).
2. System Tools: Convert, Merge, Similarity, Extractor, and Wallpaper
   (including display drag/drop, queue, and slideshow recovery).
3. Library Database plus Web Integration (crawler, requests, cloud/local sync,
   media loader, reverse search, and reconnaissance).
4. Deep Learning plus Image Stitching, Manga, and Hybrid Editor.
5. Performance/crash/recovery, credential/privacy boundaries, release notes,
   checksums, tag/publication, and post-publish smoke.

Primary manual matrix: `$REPOSITORIES_ROOT/Journal/Personal/Journals/
RELEASE_CHECKLIST_IMGTOOL.md` (also `../../Journal/Personal/Journals/
RELEASE_CHECKLIST_IMGTOOL.md` from this repo). It covers all checklist
sections, failure/cancel paths, and final sign-off; do not close an issue on a
build-only result.

Release candidates and fixture root supplied by the owner are under
`$TEST_DATA_ROOT/release-1.0.0/`: `ImageToolkit-1.0.0-x86_64.appimage`,
`image-toolkit_1.0.0_amd64.deb`, `SHA256SUMS.txt`, and `evidence/`. Key test
sets: `images/`, `video/`, `wallpaper-a/`, `wallpaper-b/`, `outputs/`,
`stitch/`, `manga/`, `models/`, `database/`, `crawler/`, `requests/`,
`cloud-sync/`, `local-directory-sync/`, `reverse-search/`, `media-loader/`,
`entity-recon/`, `comfyui/`, and `disposable/`. Use copies/writable outputs;
never mutate source fixtures or publish sensitive evidence.

Investigate release plumbing in `.github/workflows/release.yml`,
`tools/release/`, `ImageToolkit.spec`, `pyproject.toml`, `INSTALL.md`, and
`docs/CHANGELOG.md`. Capture candidate commit, artifact hash, environment,
evidence, pass/fail/blocker, and N/A reason in every issue; link defects to the
parent and keep the milestone open until their disposition and final live
verification are recorded.

— Codex

---

### Claude — 2026-09-01 (CI round-9: backend suite GREEN; GUI step hangs)

Run `33474885915` (`90cfee4e`): **`Python slow integration tests` ✅**
(exit-5 tolerance) and **"Run backend pytest" ✅ 723 passed, 9 skipped in
58s** (Grok's cv2/opencv fixes cleared the whole backend suite).

Only failure left: **"Run GUI pytest"** (`gui/test -m "not slow" --run-gui`)
**hangs at ~7%** and the job hit `timeout-minutes: 25` → cancelled.
`--timeout=120` didn't fire — the wedge is in a Qt event loop / QThread
that ignores SIGALRM (default signal method).

Fix: `--timeout-method=thread` on the GUI steps — the watchdog thread
force-dumps every stack and exits, so the next run names the offending
test (~test 65 of gui/test collection order, likely under
`gui/test/components/` — toast animation / drag-preview / gallery-stress).

— Claude

---

### Claude — 2026-09-01 (CI round-10: GUI-test hang root-caused + fixed)

`--timeout-method=thread` (run `33476692057`) named the hang: the GUI
pytest step wedged in **`gui/test/core/test_extraction_history.py`**'s
`close_settings_windows` teardown → `SettingsWindow.close()` →
`closeEvent` → `_confirm_unsaved_settings_exit()` → `QMessageBox.exec()`
— a modal "unsaved settings?" dialog that blocks forever headless.

The global `gui/test/conftest.py` teardown also calls `widget.close()` on
every top-level widget, so any test leaving a dirty `SettingsWindow` could
hang the same way.

Fix: `_close_without_modal(widget)` helper in `gui/test/conftest.py` that
sets `widget._has_unsaved_settings = lambda: False` before `close()`;
applied in both the global teardown and the local fixture. Also fixed one
stale assertion in that file — `"Theme & Aesthetics Studio"` →
`"Theme and Aesthetics Studio"` (Grok's `510be5fb` renamed the groupbox).
`pytest gui/test/core/test_extraction_history.py --run-gui` → 5 passed in
0.8s (was: 25-min hang).

Pre-existing, untouched: `gui/test/conftest.py::cleanup_active_workers_
and_timers` is ruff C901 16>15 (not enforced by the CI workflow jobs).

— Claude

---

### Claude — 2026-09-01 (CI round-11: second GUI hang — screeninfo)

Run `33478028460`: settings-modal hang fixed; GUI step got further, then
`--timeout-method=thread` named the next wedge —
**`gui/test/core/test_gallery_crash_stress.py::test_dual_linked_rapid_dir_nav`**
→ `WallpaperTab` init → `_monitor_layout.populate_monitor_layout()` →
`screeninfo.get_monitors()`, which opens an X/hardware connection and hangs
on the headless runner (no `$DISPLAY`). Only `test_core_tab.py` mocked it.

Fix: `_stub_monitor_detection` autouse fixture in `gui/test/conftest.py` —
patches `..._monitor_layout.get_monitors` to a fixed fake `Monitor` for
every gui test (only that one module calls it). Verified: crash-stress +
extraction-history + wallpaper-gallery → 21 passed, 7s (was: 25-min hang).

Also refactored `cleanup_active_workers_and_timers` (extracted
`_stop_widget_timers`) to clear a **pre-existing** ruff C901 16>15 on that
file, since these commits touch it.

Still unknown: ~5 `F` failures early in the GUI run — the thread-watchdog
hard-exit ate the summary. Next run (hangs fixed) should surface them.

— Claude

---

### Claude — 2026-09-01 (CI round-12: systemic modal-dialog stub; GUI-suite greening → Codex)

Third GUI hang (run `33478907592`): `test_tag_review_dialog.py::
test_save_all_writes_caption_sidecar_with_only_checked_tags` →
`tag_review_dialog._save_all()` → `QMessageBox.information(...)` — modal,
blocks headless. Same class as the settings-modal hang.

Stopped playing whack-a-mole: added **`_no_blocking_dialogs`** autouse
fixture in `gui/test/conftest.py` — stubs `QMessageBox.{information,warning,
critical,about,question,exec,exec_}` and `QInputDialog.getText` to
non-blocking no-ops for every gui test. 26 gui test files touch
`QMessageBox`; **0** assert on call counts, and local monkeypatch stacking
lets any test that patches a dialog itself still win. Verified:
tag_review + extraction_history → 11 passed, 0.8s.

**Three hangs now fixed** (settings unsaved-modal, `screeninfo.get_monitors`,
`QMessageBox` statics). Commits `f59caed5`, `5b4a42e1`, + this one.

**→ Codex — GUI-suite greening.** `--timeout-method=thread` + `timeout-
minutes: 25` mean any remaining hang now fails fast with a stack instead
of stalling, but there may be more (real `QThread.wait()` / `QEventLoop` /
subprocess), plus ~5 `F` failures early in the GUI collection order that
the thread-watchdog hard-exit has been eating before the summary prints.
Resolving those needs a full `gui/test/ --run-gui` run — RESOURCE RULE
territory. Backend suite is green (723 passed); `pytest-slow` green; only
the GUI half of `pytest` remains.

— Claude
