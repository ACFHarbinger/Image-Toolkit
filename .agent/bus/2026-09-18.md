### Claude — 2026-09-18 (filed #654 -- ASP product-path non-determinism, per Harbinger go-ahead)

Per Harbinger's "let us proceed" (continuation of yesterday's ASP work
session, #470/#472/#474), filed the pipeline non-determinism finding
discovered while validating #472 as its own tracking issue: **#654**.
Full evidence (9-case set churn, 3/9 flip on direct re-run, config
confound ruled out) already written up in the ASP submodule's
`docs/moon/ROADMAP.md`/`CHANGELOG.md` (commit `7a0211d`) and posted to
#472 yesterday -- #654 restates it as its own issue with a proposed
triage plan (bound the variance with repeat runs, seed-audit the
neural matching/masking stack, or redefine Ground-Rule #1 as a
tolerance band). Not started any of the proposed next steps --
flagged for triage, awaiting authorization like the rest of the ASP
RESOURCE-RULE work.

Linked #654 back from #472's comment thread.

Still open/blocked from yesterday:
- **#474** (Overmix regen): blocked on `qtbase5-dev` (needs Harbinger's
  sudo, not installed as of this check). Hugin/GT backfill separately
  determined to be near their practical ceiling (58/60 Hugin misses
  are a structural Hugin FOV-model limitation; GT backfill needs
  hand-sourced reference images, not automatable).
- **#473, #463**: still explicitly Harbinger's own queue (human rating
  pass / visual A/B review), untouched.

-- Claude

### Claude — 2026-09-18 (#474 Overmix coverage 0 -> 97/97)

Harbinger installed `qtbase5-dev` (and, as CMake surfaced each next
missing dep one at a time: `libqt5x11extras5-dev`, `libjpeg-dev`,
`libpugixml-dev`, `libraw-dev`, `libavformat-dev`/`libavcodec-dev`/
`libavutil-dev`, `libpng++-dev`, `libboost-math-dev` -- a much longer
chain than just the one package). `setup_overmix.sh` then built
`OvermixCli` cleanly.

Every regen case still failed ("OvermixCli not built") even with a
good build -- found a real bug: `run_overmix.py`'s `_TOOLKIT_ROOT` path
walk was 2 directory levels short, a leftover from before ASP became a
nested git submodule (landed at `submodules/ASP`, which has no
`vendor/` dir of its own -- `vendor/Overmix` only exists as a plain
submodule of the real Image-Toolkit root). Fixed (ASP commit
`6324153`), verified the resolved path exists, then ran the full
97-case smart-variant regen against `~/Downloads/Data/Dump`:
**97/97 succeeded, 0 failures**. Real `overmix_stitch.png` files
confirmed on disk for all 97 (was 0 before this session).

ROADMAP.md/CHANGELOG.md updated (ASP commit `e94e064`), submodule
pointer bumped in Image-Toolkit (`f81c55bd`), full write-up posted to
#474. Not closing #474 -- Hugin (37/97, 58/60 misses structural per
yesterday's investigation) and GT (55/97, needs hand-sourced reference
images) remain short of the "all 5 comparators" DoD; asked Harbinger
how they want to handle those two rather than assuming.

-- Claude

### Claude — 2026-09-18 (#474 Hugin coverage 37 -> 38/97)

Harbinger asked specifically about the Hugin borderline cases. Turned
out there's only 1, not 2 (miscounted a multi-line enblend error
message as two separate datasets earlier). Installed `hugin-tools` +
`enblend` (also missing from this machine), then diagnosed
`asp_test74`'s distinct failure: enblend's graph-cut seam optimizer
failing (`unable to run Dijkstra optimizer`), not Hugin's FOV-model
wall the other 58 hit. Added a targeted `--no-optimize` retry
(`run_hugin.py`, ASP commit `c1db809`) -- verified a real, correctly-
sized output for that case. Hugin: 37 -> 38/97.

Final #474 scorecard: Overmix 97/97, Hugin 38/97 (58 unfixable, 1
recovered), GT 55/97 (unchanged, needs hand-sourced images). Posted to
#474, asked Harbinger whether to close now or leave open pending GT.

-- Claude

### Claude — 2026-09-18 (#654 seed audit -- ASP_DETERMINISTIC=1 already fixes it)

Per Harbinger's "All available + research for improved methods" (ASP
work, continuing after #474), pursued #654's proposed "seed audit"
step. Found the fix already exists in the codebase and was just never
enabled: `configure_reproducibility()` (`manifest.py`) seeds all
RNGs + disables cudnn.benchmark + torch.use_deterministic_algorithms,
gated behind `ASP_DETERMINISTIC=1` (default off, never set during any
prior benchmark run).

Direct test: re-ran the same 9 "flaky" cases from #654's own evidence
three times with `ASP_DETERMINISTIC=1 ASP_REPRO_SEED=42` set. Runs A
and B (no CUBLAS_WORKSPACE_CONFIG) matched exactly on all 9 -- same
identity, same fallback-gate values to the decimal -- despite ~3241
CuBLAS non-determinism warnings per run. Run C added
CUBLAS_WORKSPACE_CONFIG=:4096:8: zero warnings, still matched exactly.
`ASP_DETERMINISTIC=1` alone appears sufficient in practice for this
workload.

(Mid-session note: the machine rebooted once during this investigation,
interrupting run C's first attempt. Verified no repo damage -- clean
git status, both repos on main, dump symlink intact -- and re-ran it
cleanly.)

Posted full writeup to #654, ASP commit `2a1de52`, submodule bump
`40c752bf`. Recommended (not unilaterally done) defaulting
ASP_DETERMINISTIC=1 in the benchmark justfile recipes -- awaiting
Harbinger's call, plus whether they want a full-97 confirmation run
(bigger compute ask than this 9-case test).

-- Claude

### Claude — 2026-09-18 (#654 full-97 confirmation paused -- hardware instability)

Started the approved full-97 A/B determinism confirmation. Machine
rebooted three times total during this session's heavy ASP compute
work (twice during this specific attempt). Harbinger confirmed the
third reboot was directly caused by the benchmark run, not
coincidental -- stopping bench runs here per their instruction.

Repo state verified clean after every reboot (no uncommitted work
lost, both repos on main). The 9-case ASP_DETERMINISTIC=1 confirmation
from earlier today stands; full-97 confirmation deferred until the
hardware issue (likely thermal/power under sustained GPU+CPU load) is
resolved on Harbinger's end. Posted status to #654, left open.

Moving to other implementation work per Harbinger's direction.

-- Claude
