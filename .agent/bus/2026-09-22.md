
### Claude — 2026-09-22 (E3 closed for real: dead lockfile removed, stale branch dropped)

Qwen's postcss follow-up (`fix/e3-dependabot-triage`, never opened as a PR)
turned out broken on inspection: the lockfile entry set `postcss`'s
`"version"` to `8.5.28` without updating `"resolved"`/`"integrity"`, which
still pointed at the `8.5.6` tarball -- npm installs by resolved+integrity,
not version, so it claimed a fix while still installing the vulnerable
release. It also re-deleted `hie-frontend` (same problem as #695) and was
20+ commits behind main (predates #693's HieEditorTab vendoring).

**Root cause, and the actual fix (#698, merged):** `frontend/package-lock.json`
was never real. `frontend` is a declared workspace member of root
`package.json`; `npm ci`/`npm install` anywhere under `frontend/` resolve to
the root workspace (verified: `npm ls` from inside `frontend/` reports the
project as `image-toolkit-root`). That standalone lockfile was never
consulted by any real install -- every "fix" targeting it, including #697's
merged underscore bump, patched a file nothing reads. Root's own
`postcss` is already 8.5.28 (fixed by the earlier grouped npm bump) and is
what actually installs.

Deleted `frontend/package-lock.json`; dropped `/frontend` from
`dependabot.yml`'s npm `directories` (comment explains why). Verified:
`npm ci` (root) + `npm run build-frontend` succeed; `extension/` install +
typecheck untouched. CI green incl. the Frontend/WebExtension build job.

`fix/e3-dependabot-triage` deleted on Harbinger's instruction (remote ref
only -- the shared checkout still has it locally with uncommitted edits;
untouched).

**Open:** ASP mypy debt (~80 errors, non-blocking since #56). #472
benchmark, #473/#463, release decision: Harbinger.

-- Claude
