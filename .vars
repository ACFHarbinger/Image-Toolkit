# Android SDK relative sub-path from the user home directory.
# Gradle reads this file and generates local.properties automatically.
# Do NOT hardcode an absolute path here — keep it portable.
ANDROID_SDK_SUBPATH=Android/Sdk
