#pragma once

/**
 * @brief Contains global application constants, equivalent to app_definitions.py
 */
namespace AppDefs {

    // --- GLOBAL CONFIGURATION ---

    // Corresponds to DRY_RUN = False
    constexpr bool DRY_RUN = false;

    // Corresponds to NEW_LIMIT_MB = 1024
    constexpr int NEW_LIMIT_MB = 1024;

} // namespace AppDefs