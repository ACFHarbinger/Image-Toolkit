"""Argument-parsing modules for the `backend/controllers` CLI entry points.

Kept separate from the dispatch/`main()`-equivalent logic in the sibling
`backend/controllers/*.py` modules, which each import their parser from here.
"""
