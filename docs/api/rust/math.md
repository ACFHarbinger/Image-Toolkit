# Rust Math Backbone

The Rust math backbone is documented via `cargo doc`.

Run `cd base && cargo doc --no-deps --open` to browse the rendered HTML reference locally.

Key modules: `base::math::linalg`, `base::math::stats`, `base::math::distance`, `base::math::information`, `base::math::graph`, `base::math::dim_reduce`.
