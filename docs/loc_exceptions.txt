# Image-Toolkit — Code Line-Count Limit Exceptions (Max 500 code lines)
# Tracking issue: GitHub #626 / Architecture §5.17
# Format: Relative path to python file (from workspace root or search path).
#         Comments (#) and empty lines are ignored.

# Permanent exception: stage-by-stage HITL pipeline run() threading state
# across 13 stages — deliberately not split further per module docstring.
gui/src/helpers/animation/stitch_worker/_progress_pipeline.py

# Temporary exceptions — none currently (#629 splits all landed).
