"""Quarantined UI prototype components (Issue #524 / Phase 0.4).

These prototype modules (navigation shell, context inspector, telemetry bar,
and declarative module registry) are isolated here so they do not get pulled
into the live shell via wildcard or eager imports.
"""
