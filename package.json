{
  "name": "image-toolkit-root",
  "version": "1.0.0",
  "description": "Root package for the Image Toolkit monorepo.",
  "private": true,
  "license": "UNLICENSED",
  
  "workspaces": [
    "frontend/web"
  ],

  "dependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0",
    "wait-on": "^7.2.0"
  },
  
  "scripts": {
    "start-all": "npm run dev -w web-app",
    "install": "npm install"
  }
}